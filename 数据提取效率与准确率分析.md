# 数据提取效率与准确率分析

> 分析对象: ApexCrawler v0.2.0 | 分析维度: 效率(速度) / 准确率 / 成本

---

## 效率优化建议 (速度) — 具体到方法级别

### 1. 分层降级策略 — 当前为顺序管线, 非真正分层

**现状**: PipelineExecutor 按 Schedule→Route→Evade→Extract 串行执行, 无快速路径。DecisionEngine.recommend() 默认 `entry_point: "http"`, 但实际 Pipeline 不读取此字段。

**问题**: 无真正意义的"先试 API → 失败降 HTTP → 再降浏览器"短路机制。

**建议**:
```python
# 在 Stage 0 (Schedule) 之前增加 FastProbeStage
class FastProbeStage:
    """快速探测: 并行尝试 移动API → JSON端点 → RSS → HTTP HEAD → 浏览器"""
    
    async def execute(self, ctx: PipelineContext) -> PipelineContext:
        # 优先级: 移动API > 嗅探API > HTTP HEAD > 浏览器
        probes = [
            ("mobile_api", self._probe_mobile_api),    # 50ms
            ("sitemap_rss", self._probe_sitemap_rss),  # 80ms
            ("json_endpoint", self._probe_json_endpoint), # 60ms
        ]
        # 并行探测, 任意一个命中即返回
        for name, probe in probes:
            result = await asyncio.wait_for(probe(ctx.target_url), timeout=0.5)
            if result and result.confidence > 0.7:
                ctx.direct_data = result.data
                ctx.bypass_browser = True  # 跳过浏览器
                return ctx
        return ctx
```

**最佳实践**:
- **移动端 API** 优先级最高: 用 `m.` / `mobile.` 子域名 + `User-Agent: Android/Chrome` 重试。移动 API 通常返回 JSON, 比桌面端 HTML 小 70-90%, 且反爬较弱。
- **sitemap.xml / RSS / Atom 订阅**: 许多网站同步数据到 sitemap, 直接解析结构化 XML。
- **浏览器渲染跳过条件**: 当 `ctx.bypass_browser=True` 或目标内容在首屏 500KB 文本内, 直接走 HTTP 提取。

### 2. 优先级矩阵: 哪些场景可跳过完整浏览器渲染

| 场景 | 可跳过? | 方法 | 预计提速 |
|------|---------|------|---------|
| JSON API / GraphQL 端点 | ✅ 直接请求 | `httpx.get(api_url)` | 10-50x |
| 静态 HTML (SSR) | ✅ HTTP 抓取 | curl_cffi + HTMLParser | 5-10x |
| OpenGraph/JSON-LD 数据充足 | ✅ 仅取 `<head>` | `Range` 请求或截断 | 3-5x |
| 需要 JS 渲染内容 | ❌ | 浏览器必需 | 基准 |
| 字体加密页面 | ❌ | 需完整 DOM+CSSOM | 基准 |
| 需要验证码交互 | ❌ | 需 Humanizer | - |

**代码修改建议** (`ai_extractor.py:_trim_html`):
```python
# 当前: 硬截断 8000 字符, 丢失尾部内容
html[:8000]
# 
# 改进: 智能提取关键区域
def _smart_trim(self, html: str) -> str:
    # 1. 保留 <head> (JSON-LD, meta tags)
    # 2. 保留 <main>, <article>, [role="main"]
    # 3. 移除 <nav>, <footer>, <aside>
    # 4. 按语义区域排序, 头部权重高
    pass
```

### 3. 移动端 API 优先级策略

**为什么值得**: 移动端 API 通常:
- 返回 JSON (不需 HTML 解析)
- 反爬措施较弱 (移动端验证困难)
- 数据密度高 (不含导航/广告/推荐)

**实现** (`routing/matcher.py` 集成):
```python
MOBILE_DOMAINS = ["m.", "mobile.", "api.", "touch.", "wap.", "h5."]

def try_mobile_endpoint(self, url: str) -> str | None:
    """从桌面 URL 推导移动 URL, 并行探测"""
    parsed = urlparse(url)
    candidates = [f"{parsed.scheme}://api.{parsed.netloc}{parsed.path}"]
    candidates += [f"{parsed.scheme}://m.{domain_remove_subdomain(parsed.netloc)}{parsed.path}"]
    # 并行 HEAD 请求, 返回首个 200/301 的 URL
```

### 4. 并行抓取 vs 串行 — 最佳实践

**现状**: EnginePool 支持 `max_concurrent`, 但 PipelineExecutor.run() 是串行的。

**改进**:
```python
# 批量爬取模式: 多 URL 共享同一引擎实例
class BatchPipelineExecutor(PipelineExecutor):
    async def run_batch(self, urls: list[str], ctx_template: PipelineContext) -> list[tuple[bool, PipelineContext]]:
        """同一引擎实例处理多个 URL, 复用连接池/Cookie/DNS"""
        semaphore = asyncio.Semaphore(self._max_parallel)
        async def _process(url):
            async with semaphore:  # 控制并发
                ctx = ctx_template.copy()
                ctx.target_url = url
                return await self.run(ctx)
        return await asyncio.gather(*[_process(u) for u in urls])
```

**并行建议**:
- **同域名**: HTTP/2 多路复用, 1 连接承载 100+ 并发流, 无需多连接
- **跨域名**: 独立连接池, 每域名 6 连接 (HTTP/1.1) 或 1 连接 (HTTP/2)
- **引擎池并发**: Vanilla 3-5 并发, Cloaked/Camoufox 1-2 并发 (资源重)
- **DNS 预热**: 对已知域名提前解析, 消除首次 DNS 延迟 (50-200ms)

### 5. 网络层优化 — 当前全部缺失 (P0)

**HTTP/2 多路复用**:
```python
# http/backends/curl_cffi.py 增加
import curl_cffi.requests
session = curl_cffi.requests.Session(
    impersonate="chrome124",
    http_version="h2",  # 强制 HTTP/2
    max_clients=100,    # 连接复用
)
```

**连接池管理** (新文件 `http/connection_pool.py`):
```python
class ConnectionPool:
    """Per-origin 连接管理, 模拟 Chrome 行为"""
    _pools: dict[str, list]  # origin → [conn1, conn2, ...]
    
    async def get_connection(self, origin: str) -> Connection:
        """每个 origin 维护 1-6 个连接, HTTP/2 用 1 个"""
        pool = self._pools.get(origin, [])
        for conn in pool:
            if conn.is_reusable():
                return conn  # 连接复用
        conn = await self._create_connection(origin)
        if len(pool) < self._max_conns_per_origin:  # 默认 6
            pool.append(conn)
        return conn
```

**DNS 缓存**:
```python
# 内置 DNS 缓存 (10 分钟 TTL), 避免每次请求解析
import socket
_dns_cache: dict[str, tuple[str, float]] = {}

def cached_getaddrinfo(host: str) -> str:
    if host in _dns_cache:
        ip, ts = _dns_cache[host]
        if time.monotonic() - ts < 600:
            return ip
    ip = socket.getaddrinfo(host, 443)[0][4][0]
    _dns_cache[host] = (ip, time.monotonic())
    return ip
```

### 6. 缓存策略优化

| 缓存类型 | 当前 TTL | 建议 TTL | 命中率目标 |
|---------|---------|---------|----------|
| 决策引擎 (decision) | 300s | 600s (10min, 加 LRU 淘汰) | 90%+ |
| 字体映射 (font) | 86400s | 永久 (SHA256 去重) | 99%+ |
| **TLS Session** | ❌ 无 | 3600s (RFC 8446 允许复用) | 40-60% |
| **页面指纹** | ❌ 无 | 300s (同 URL + 同时间段) | 30-50% |
| **HTML 内容哈希** | ❌ 无 | 基于 ETag/Last-Modified | 20-40% |
| **DNS 缓存** | ❌ 无 | 600s | 95%+ |
| **代理 IP 评分** | ❌ 无 | 86400s (24h 冷却) | 减少 40% 封禁 |

**TLS Session 缓存实现** (新增):
```python
# http/tls_cache.py
class TLSSessionCache:
    """缓存 TLS 1.3 session tickets, 减少握手 1 RTT"""
    _cache: dict[str, bytes] = {}
    
    async def get_session(self, host: str) -> bytes | None:
        return self._cache.get(host)
    
    async def save_session(self, host: str, ticket: bytes):
        self._cache[host] = ticket  # 重复访问时复用
        # 可节省 ~40ms per request (减少 1 RTT 的 Hello 交换)
```

**HTTP 条件请求** (新增):
```python
async def fetch_with_etag(url, cached_etag=None):
    """If-None-Match → 304 Not Modified → 跳过下载"""
    headers = {}
    if cached_etag:
        headers["If-None-Match"] = cached_etag
    resp = await session.get(url, headers=headers)
    if resp.status_code == 304:
        return None, cached_etag  # 内容未变, 用缓存
    return resp.text, resp.headers.get("ETag")
```

---

## 准确率优化建议 — 具体到 Prompt/参数级别

### 1. LLM 提取准确率瓶颈 (6 个方向)

**瓶颈 #1: Prompt 工程过于简陋** (`ai_extractor.py:_build_prompt`)
```python
# 当前: 无 Few-Shot, 无 Chain-of-Thought, 无 confidence 要求
prompt = """Extract structured data from this HTML. Return ONLY valid JSON..."""
```

**改进后的 Prompt 模板**:
```python
EXTRACTION_PROMPT_v2 = """You are a web data extractor. Extract structured data from HTML.

## Schema (Pydantic):
{schema_fields}

## Examples (Few-Shot):
{few_shot_examples}  # ← 关键改进: 2-3 个示例

## HTML Content:
{trimmed_html}

## Instructions:
1. Identify the main content area (product/article/profile)
2. For each field, find the EXACT location in HTML first
3. If a field is missing, use null (never guess)
4. For prices: extract NUMERIC only (e.g. "$19.99" → 19.99)
5. For dates: use ISO 8601 format (YYYY-MM-DD)
6. Return ONLY valid JSON, no markdown, no explanation

## Chain-of-Thought:
Think step by step:
- Step 1: What type of page is this? (product/article/profile/listing)
- Step 2: Where is the main content container?
- Step 3: Extract each field with confidence score

Return:
```json
{{
  "fields": {{ ... }},
  "confidence_scores": {{ ... }},  // per-field 0.0-1.0
  "extraction_method": "semantic|html|meta|fallback"
}}
```"""

# 参数设置
LLM_EXTRACTION_CONFIG = {
    "temperature": 0.0,       # 确定性输出 (提取任务不需要创造性)
    "top_p": 0.1,             # 极低 randomness
    "max_tokens": 1024,       # 结构化数据不需要长输出
    "response_format": {"type": "json_object"},  # 强制 JSON
}
```

**瓶颈 #2: HTML 截断丢失关键信息**
```python
# 当前: 8000 字符从头部截断, 后半部分内容完全丢失
html[:8000]

# 改进: 语义分块 + 优先级排序
def _semantic_trim(self, html: str, max_chars: int = 12000) -> str:
    """保留语义关键区域, 而非简单截断"""
    from bs4 import BeautifulSoup
    soup = BeautifulSoup(html, 'lxml')
    
    priority_regions = [
        ('script[type="application/ld+json"]', 10),  # 结构化数据, 最高权重
        ('meta[property^="og:"]', 8),                # OpenGraph
        ('meta[name^="twitter:"]', 7),               # Twitter Card
        ('[itemtype*="schema.org"]', 9),             # Schema.org 微数据
        ('main, article, [role="main"]', 8),          # 语义主区域
        ('h1, h2, h3, .title, .heading', 6),         # 标题
        ('[class*="price"], [class*="content"]', 5),  # 价格/内容区域
    ]
    # 按优先级组装, 优先保留高权重节点
```

**瓶颈 #3: 无置信度反馈机制**
```python
# 增加 per-field confidence
class ExtractionResult(BaseModel):
    fields: dict[str, Any]
    confidence_scores: dict[str, float]
    extraction_method: str  # "semantic" | "html_selectors" | "meta" | "fallback"

# 在 ValidateStage 后增加 confidence gate
if extraction.confidence_scores.get("price", 0) < 0.5:
    ctx.retry_extraction = True
    ctx.retry_hint = "price field confidence low, check .price/.amount selectors"
```

### 2. 多源交叉验证 — 当前缺失

**实现方案** (`extraction/cross_validator.py`):
```python
class CrossValidator:
    """从多个数据源提取同一字段, 交叉验证"""
    
    SOURCES = ["llm_semantic", "json_ld", "meta_tags", "microdata", "xpath_css"]
    
    async def validate(self, html: str, field: str, candidates: dict[str, Any]) -> VerifiedValue:
        """多源投票: 2/3 一致则接受"""
        sources = {}
        sources["json_ld"] = self._extract_from_jsonld(html, field)
        sources["meta"] = self._extract_from_meta(html, field)
        sources["microdata"] = self._extract_from_microdata(html, field)
        sources["llm"] = candidates.get(field)
        
        # 投票逻辑: 至少 2 个来源给出相同值
        from collections import Counter
        values = [s for s in sources.values() if s is not None]
        counts = Counter(values)
        majority_value, count = counts.most_common(1)[0]
        
        return VerifiedValue(
            value=majority_value,
            confidence=count / len(values),
            sources_agree=count >= 2,
            all_sources=sources,
        )
```

**交叉验证价值评估**:
- ✅ **价格字段**: 高价值。JSON-LD 价格 vs 页面显示价格经常不一致 (含税/不含税)
- ✅ **日期字段**: 高价值。OpenGraph vs Schema.org vs 页面显示可能差 1 天
- ⚠️ **描述文本**: 中等价值。来源差异大, 投票可能无法收敛
- ❌ **图片 URL**: 低价值。来源差异通常是正常变体

### 3. 自愈 XPath 重定位 — 成功率提升

**当前状态**: 方案文档提到, 代码未实现。

**成功率影响因素**:
```
成功率 = f(页面结构稳定性, 选择器鲁棒性, 备用路径数量)

页面不变:   ID选择器 100%
轻度变更:   CSS class选择器 85-95%
中度变更:   语义选择器 70-85%
重度变更:   文本匹配 50-70%
完全重构:   需要 LLM 重新分析 20-40%
```

**实现** (`extraction/sel_healer.py`):
```python
class SelfHealingSelector:
    """存储多重冗余选择器, 自动从失败中恢复"""
    
    def __init__(self, html: str):
        self._tree = lxml.html.fromstring(html)
    
    def generate_redundant_selectors(self, element) -> list[str]:
        """为一个元素生成 5 种选择器"""
        selectors = []
        # 1. ID (最强)
        if element.get('id'):
            selectors.append(f"#{element.get('id')}")
        # 2. 唯一 class 组合
        classes = element.get('class', '').split()
        if classes:
            selectors.append("." + ".".join(classes[:3]))
        # 3. XPath with text
        text = element.text_content().strip()[:50]
        if text:
            selectors.append(f"//*[contains(text(), '{text}')]")
        # 4. Data attributes
        for attr in element.attrib:
            if attr.startswith('data-'):
                selectors.append(f"[{attr}='{element.get(attr)}']")
        # 5. Semantic path
        selectors.append(self._build_semantic_path(element))
        return selectors
    
    def heal(self, failed_selector: str) -> tuple[str, float]:
        """从失败的选择器恢复, 返回(新选择器, 相似度)"""
        # 1. ID 失败 → 尝试 class 组合
        # 2. Class 失败 → 尝试文本匹配  
        # 3. 文本失败 → LLM 重新分析 (需缓存结果)
        pass
```

### 4. 字体反爬 — FontTools vs OCR 决策矩阵

**当前实现** (`font_cracker.py`): FontTools → OCR 直线降级, 无决策逻辑。

**改进决策矩阵**:
```python
FONT_STRATEGY_DECISION = {
    # (字体类型, 编码方式) → 最佳方法
    
    # 标准替换字体 (猫扑/大众点评): WOFF+cmap table 被替换
    ("woff2", "glyph_substitution"): "FontTools",  # cmap 解析, 100% 准确
    
    # 动态字形 (58同城): 字形随机生成
    ("woff2", "dynamic_glyph"): "OCR",   # 需 OCR, 85-95% 准确
    
    # CSS unicode-range 混淆
    ("css", "unicode_fragmentation"): "FontTools",  # 解析 css, 拼接映射
    
    # WASM 运行时编码 (最高难度)
    ("wasm", "runtime_encoding"): "CDP_WASM_Hook",  # 拦截 WASM 函数调用
    
    # SVG 字形矢量编码
    ("svg", "path_encoding"): "PathMatcher",  # SVG path 相似度匹配
}

# 选择逻辑
def select_strategy(self, font_url: str) -> str:
    font_type = detect_font_type(font_url)
    encoding = detect_encoding_pattern(self.html)
    return FONT_STRATEGY_DECISION.get((font_type, encoding), "FontTools")
```

**OCR 准确率提升**: 当前 ddddocr 不适用于字体场景 (它针对验证码)。应改用:
```python
# 字体 OCR 专用方案
ocr_engine = PaddleOCR(lang='ch', use_angle_cls=True, det_db_thresh=0.3)
# 对渲染的单个字形截图做 OCR, 精度 95%+
# 两阶段: 1. 检测字形框 2. 识别文字
```

### 5. 结构化数据验证最佳实践

**当前** (`ValidateStage`): 仅 Pydantic model_validate, 无类型检查/范围检查。

**改进** (`extraction/validate.py`):
```python
class Validator:
    """多层次数据验证"""
    
    VALIDATION_RULES = {
        "price": [
            (lambda v: v > 0, "价格必须 > 0"),
            (lambda v: v < 10_000_000, "价格超出合理范围"),
            (lambda v: isinstance(v, (int, float)), "价格必须是数字"),
        ],
        "date": [
            (lambda v: datetime.fromisoformat(v) > datetime(2000,1,1), "日期不能早于 2000 年"),
            (lambda v: datetime.fromisoformat(v) <= datetime.now(), "日期不能是未来"),
        ],
        "rating": [
            (lambda v: 0 <= v <= 5, "评分必须在 0-5 之间"),
        ],
        "stock": [
            (lambda v: v in ["in_stock", "out_of_stock", "pre_order"], "库存状态无效"),
            (lambda v: isinstance(v, str) and len(v) < 50, "库存状态过长"),
        ],
    }
    
    def validate_field(self, field: str, value: Any) -> list[str]:
        """返回验证错误列表"""
        errors = []
        # 1. 类型检查
        rules = self.VALIDATION_RULES.get(field, [])
        for check_fn, error_msg in rules:
            try:
                if not check_fn(value):
                    errors.append(f"[{field}] {error_msg}: got {value}")
            except Exception as e:
                errors.append(f"[{field}] 验证异常: {e}")
        
        # 2. 空值处理
        if value is None or value == "" or value == "N/A":
            errors.append(f"[{field}] 空值 — 标记为需重试")
        
        # 3. 格式检查
        if field.endswith("_url") and value:
            if not value.startswith(("http://", "https://", "//")):
                errors.append(f"[{field}] URL 格式无效: {value}")
        
        return errors
```

### 6. JSON-LD / Schema.org / OpenGraph 优先级策略

**优先级**: JSON-LD > Microdata (schema.org) > OpenGraph > Twitter Card > Meta Keywords

```python
STRUCTURED_DATA_PRIORITY = [
    # (提取源, 权重, 解析复杂度)
    ("json_ld", 1.0, "low"),        # 结构化 JSON, 最可靠
    ("microdata", 0.85, "medium"),   # HTML 属性, 次可靠
    ("opengraph", 0.7, "low"),      # Meta 标签, 字段有限
    ("twitter_card", 0.6, "low"),   # 类似 OG, 更少字段
    ("meta_keywords", 0.2, "low"),  # 最不可靠
]
```

**合并策略**: 按优先级合并, 高优先级覆盖低优先级, 相同字段不重复提取。

---

## 成本优化建议 — 具体到数字

### 1. 最小化 LLM API 调用次数

**当前成本分析** (假设 GPT-4o-mini):
- AIExtractor: 每次提取 ~3K input tokens + ~300 output tokens = ~3.3K tokens
- DecisionEngine: 每次决策 ~200 input tokens + ~100 output tokens = ~300 tokens (L2 only)
- 假设每日 10,000 次请求, 75% 命中 L0 缓存:
  - 需要 LLM 调用的: 10,000 × 25% × (3.3K + 0.3K) = 9,000K tokens
  - 成本: 9M tokens × $0.15/M = **$1.35/天**

**优化目标**: 降至 **$0.30/天** (减少 78%)

**方案**:

| 策略 | 节省量 | 实现方式 |
|------|--------|---------|
| HTML 预处理裁剪 | 40% | 智能 trim → 1.8K tokens (vs 3K) |
| 结构化数据优先 | 30% | JSON-LD 命中则跳过 LLM 提取 |
| Schema 字段过滤 | 15% | 只提取目标字段, 不提取全 Schema |
| 响应缓存 (URL+SHA256) | 20% | 相同 HTML 内容不重复调用 LLM |
| L1 小模型过滤 | 50% | 本地模型判断"是否需要 LLM" |

**实现** (`ai_extractor.py`):
```python
async def extract(self, html: str, schema: type[T]) -> T:
    # 0. 内容哈希去重
    html_hash = hashlib.sha256(html.encode()).hexdigest()
    if cached := self._cache.get(f"extract:{html_hash}"):
        return cached
    
    # 1. 结构化数据优先 (零成本)
    try:
        return self._extract_from_jsonld_or_microdata(html, schema)
    except ExtractionError:
        pass
    
    # 2. L1 本地模型预判 (低成本)
    if self._local_model and self._local_model.should_call_llm(html, schema):
        return self._local_model.extract(html, schema)
    
    # 3. L2 LLM 提取 (最高成本, 但 HTML 已高效修剪)
    trimmed = self._semantic_trim(html, max_chars=4000)  # 减少到 4K chars
    result = await self._llm.extract(trimmed, schema)
    
    # 4. 缓存结果
    self._cache.set(f"extract:{html_hash}", result, ttl=3600)
    return result
```

### 2. 智能 HTML 预处理 — 最佳裁剪比例

| 裁剪级别 | 去除内容 | 大小减少 | 适用场景 |
|---------|---------|---------|---------|
| **L0 无损** | comments, whitespace | 15-25% | 所有场景 |
| **L1 轻度** | script, style, noscript | 30-45% | 90% 场景 |
| **L2 中度** | nav, footer, aside, iframe | 50-65% | 内容页提取 |
| **L3 激进** | svg, canvas, video, audio, 冗余属性 | 70-85% | LLM 提取专用 |

```python
# 自适应裁剪: 根据页面大小动态选择级别
def adaptive_trim(self, html: str, target_tokens: int = 2000) -> str:
    current_chars = len(html)
    
    if current_chars < 4000:  # 小型页面, 不裁剪
        return html
    
    levels = [
        (0.85, self._trim_L0),  # 保留 85%
        (0.70, self._trim_L1),  # 保留 70%
        (0.50, self._trim_L2),  # 保留 50%
        (0.30, self._trim_L3),  # 保留 30%
    ]
    
    for ratio, trim_fn in levels:
        trimmed = trim_fn(html)
        if len(trimmed) <= target_tokens * 4:  # 乘以 4 (char/token 比例)
            return trimmed  # 达到目标, 停止裁剪
    
    return trimmed  # 最终裁剪结果
```

### 3. 多引擎切换触发条件

**当前**: 仅通过 403/429 状态码 + cookie/header 厂商签名。

**改进信号检测矩阵**:

```python
ENGINE_SWITCH_SIGNALS = {
    # (信号, 阈值, 触发动作)
    "http_403": (True, "立即切换到浏览器引擎"),
    "http_429": (True, "HTTP 引擎 + 加长延迟, 3次后切换"),
    "http_503": (True, "切换 IP + 浏览器引擎"),
    
    "empty_body": (len(html) < 500, "可能被 WAF 拦截, 切浏览器"),
    "captcha_detected": (True, "需要浏览器 + Humanizer"),
    
    "js_challenge": ("_cf_chl_opt" in html, "Cloudflare JS Challenge, 切 Camoufox"),
    "font_encoded": ("@font-face" in html and "base64" in html, "启用 FontCracker"),
    "wasm_present": ("application/wasm" in html, "切 WASM 拦截引擎"),
    
    "timeout_3x": (consecutive_timeouts >= 3, "引擎不可用, 切换备用引擎"),
    "high_failure_rate": (failure_rate > 0.3, "降级到更稳定的引擎"),
    "response_size_anomaly": (size < expected * 0.1, "内容被截断/屏蔽"),
}
```

**触发流程**:
```
Request → [Success] → 继续当前引擎
       → [403/503] → 立即切换 (高优先级)
       → [Timeout ×2] → 引擎降级
       → [Empty/Captcha] → 引擎升级
       → [Content Anomaly] → 切换 + 标记 URL
```

### 4. 成本数字对比

| 优化项 | 优化前 | 优化后 | 节省 |
|-------|--------|--------|------|
| LLM Token 消耗/天 (10K req) | 9M tokens | 2.5M tokens | -72% |
| LLM API 成本/天 | $1.35 | $0.38 | **-$0.97/天** |
| HTML 传输大小 | 150KB avg | 45KB avg | -70% |
| 网络带宽/天 (10K req) | 1.5GB | 0.45GB | -1.05GB |
| 字体缓存命中率 | ~50% (仅内存) | ~95% (Redis+L1) | -45% 重复下载 |
| 内存占用 (缓存) | ~50MB | ~200MB (含字体/页面) | +150MB (可接受) |

---

## 推荐实施优先级

### P0 (本周 — 最大 ROI)

| # | 任务 | 文件 | 效果 |
|---|------|------|------|
| 1 | **实现结构化数据优先提取** (JSON-LD → Microdata → OpenGraph → LLM) | `extraction/ai_extractor.py` | 减少 40% LLM 调用 |
| 2 | **改进 Prompt 工程** (Few-Shot + CoT + temperature=0 + JSON mode) | `extraction/ai_extractor.py` | 准确率 +15-25% |
| 3 | **智能 HTML 裁剪** (语义分块 → 保留优先区域 → 自适应截断) | `extraction/ai_extractor.py` | Token 节省 50% |
| 4 | **HTTP/2 + 连接池** (curl_cffi session + per-origin 连接管理) | `http/connection_pool.py` (新增) | 速度 +2-3x |
| 5 | **多引擎切换信号增强** (增加空响应/验证码/JS挑战检测) | `decision/engine.py` | 成功率 +10-15% |

### P1 (下周 — 巩固能力)

| # | 任务 | 文件 | 效果 |
|---|------|------|------|
| 6 | **FontTools/OCR 决策矩阵** (按字体类型自动选择) | `anti_font/font_cracker.py` | 字体准确率 +20% |
| 7 | **TLS Session 缓存** (session ticket 复用) | `http/tls_cache.py` (新增) | 每请求节省 ~40ms |
| 8 | **DNS 缓存** (内置 10min TTL) | `http/connection_pool.py` | 消除 50-200ms 首次延迟 |
| 9 | **多源交叉验证** (JSON-LD + LLM + microdata 投票) | `extraction/cross_validator.py` (新增) | 价格准确率 +10% |
| 10 | **内容哈希去重** (同 HTML 不重复 LLM 提取) | `extraction/ai_extractor.py` | 减少 20% 重复调用 |

### P2 (下月 — 长期投入)

| # | 任务 | 文件 | 效果 |
|---|------|------|------|
| 11 | **自愈 XPath 重定位** (5 种冗余选择器 + 自动恢复) | `extraction/sel_healer.py` (新增) | 减少 60% 提取失败 |
| 12 | **L1 本地模型** (用于决策和提取预判) | `decision/model.py` (新增) | LLM 调用再减 50% |
| 13 | **移动端 API 嗅探** (自动推导 API 端点) | `routing/matcher.py` | 新增快速路径 |
| 14 | **PaddleOCR 集成** (替代 ddddocr 做字体 OCR) | `anti_font/ocr_engine.py` | 字体 OCR 准确率 85%→95% |
| 15 | **批量 Pipeline** (同引擎多 URL 并行) | `pipeline/core.py` | 批量吞吐 +3-5x |

---

## 总结

| 维度 | 当前评分 | 目标评分 | 关键差异 |
|------|---------|---------|---------|
| **抓取效率** | 4/10 | 7/10 | 缺少分层降级/连接池/HTTP2/DNS缓存 |
| **数据准确率** | 5/10 | 8/10 | Prompt简陋/无交叉验证/无自愈选择器/OCR虚标 |
| **成本控制** | 3/10 | 7/10 | LLM过度调用/HTML裁剪不智能/缺内容去重 |

**核心原则**: 
1. **先结构化后语义** — JSON-LD 能解决的, 绝不调用 LLM
2. **先本地后远程** — 缓存/L1模型/规则 覆盖 90% 场景
3. **先移动后桌面** — 移动端 API 优先尝试 (JSON + 弱反爬)
4. **先 HTTP/2 后浏览器** — 连接开销从 200ms 降到 20ms
